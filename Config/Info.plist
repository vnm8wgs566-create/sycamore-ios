<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Sycamore</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!--
	  The export-compliance answer, given once here instead of in a dialog on every upload.

	  False because the app implements no encryption of its own. Everything it protects, it
	  protects with the operating system's: `URLSession` over https to Supabase, and Sign in with
	  Apple through `AuthenticationServices`. There is no CryptoKit, no CommonCrypto, no Security
	  framework and no third-party package in the build — `ZIPArchive` is the one file that says
	  the word, and only to *refuse* an encrypted member rather than to open one.

	  Using Apple's encryption is exactly what the fourth answer in Xcode's dialog means by "none
	  of the algorithms mentioned above". If the app ever hashes, signs, or seals something
	  itself, this key stops being true and the answer has to be re-thought rather than re-typed.
	-->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Pick a photo for your profile so the people you work with can put a face to your name.</string>
	<key>UIAppFonts</key>
	<array>
		<string>InstrumentSans[wdth,wght].ttf</string>
		<string>Newsreader[opsz,wght].ttf</string>
	</array>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
</dict>
</plist>
